1. Numbers
Mean - Average
Median - Sort the array and the middle element is the Median
Mode - The number that occurs more often than others

Property of Median
The numbers lesser than midian might not be equal to numbers greater than median in cases where median repeats.
So median only exist has two conditions
Either smaller_numbers == larger_numbers == half
Or smaller_numbers < half and smaller_numbers + count(median_element) > half 

Why greater and not greater than equal? because count also takes the median element thus forming more than half

Ex: Q1
